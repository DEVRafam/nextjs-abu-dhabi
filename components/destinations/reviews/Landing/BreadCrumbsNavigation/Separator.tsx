// Tools
import { styled } from "@mui/system";
// Types
import Box from "@mui/material/Box";

export default styled(Box)(({ theme }) => ({
    margin: "0 5px",
    fontSize: "1.2rem",
    userSelect: "none",
}));
